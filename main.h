#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdlib.h>
#include <stdarg.h>
#include <unistd.h>

int printchar(char c);
int _printf(const char *format, ...);
int putstr(char *str);

#endif /* MAIN_H */
